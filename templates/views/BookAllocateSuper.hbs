<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Book Allocation </title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(to right, #dbeafe, #e0f2fe);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    .glass-card {
      margin-top: 5vw;
      background: rgba(255, 255, 255, 1);
      backdrop-filter: blur(14px);
      border-radius: 20px;
      box-shadow: 0 12px 25px rgba(0, 0, 0, 0.15);
      padding: 2.5rem;
      width: 100%;
      max-width: 600px;
      border: 1px solid rgba(255, 255, 255, 0.25);
    }

    .form-title {
      font-size: 2rem;
      font-weight: bold;
      text-align: center;
      margin-bottom: 1.5rem;
      color: #0d47a1;
    }

    label {
      font-weight: 600;
      color: #374151;
    }

    .form-control {
      border-radius: 0.7rem;
    }

    .form-control:focus {
      border-color: #0d47a1;
      box-shadow: 0 0 0 0.15rem rgba(13, 71, 161, 0.2);
    }

    .AllocateBtn {
      background-color: #0d47a1;
      border: none;
      padding: 0.75rem 2rem;
      font-weight: 600;
      color: white;
      border-radius: 0.75rem;
      transition: background-color 0.3s ease;
    }

    .AllocateBtn:hover {
      background-color: #08306b;
    }

    .form-group {
      margin-bottom: 1.25rem;
    }
  </style>
</head>
<body>

  {{> SuperAdminNav}}

  <div class="glass-card">
    <div class="form-title">📚 Allocate a Book</div>
    <form action="/BookAllocate" method="POST">
      
      <div class="form-group">
        <label for="isbn">Book ISBN</label>
        <input type="number" class="form-control" id="isbn" name="isbn" required>
      </div>

      <div class="form-group">
        <label for="registrationNo">Student Registration No.</label>
        <input type="text" class="form-control" id="registrationNo" name="registrationNo" required>
      </div>

      <div class="form-group">
        <label for="IssueDate">Issue Date</label>
        <input type="date" class="form-control" id="IssueDate" name="IssueDate" required>
      </div>

      <div class="form-group">
        <label for="ReturnDate">Return Date</label>
        <input type="date" class="form-control" id="ReturnDate" name="ReturnDate" required>
      </div>

      <div class="text-center mt-4">
        <button type="submit" class="AllocateBtn">Allocate Book</button>
      </div>
    </form>
  </div>

 </body>
</html>
